# open-karte デザインシステム

shadcn/ui + Tailwind CSS ベースのデザイン規約。コンポーネントの使い方・トークン・レイアウトパターンを定義する。

## デザイントークン

`globals.css` の CSS 変数で管理。oklch カラースペースを使用。

### カラーパレット

#### ベースカラー

| トークン               | 用途                     | Light                            | Dark                             |
| ---------------------- | ------------------------ | -------------------------------- | -------------------------------- |
| `--background`         | ページ背景               | `oklch(1 0 0)` 白                | `oklch(0.147 0.004 49.3)` ほぼ黒 |
| `--foreground`         | テキスト                 | `oklch(0.147 0.004 49.3)` ほぼ黒 | `oklch(0.986 0.002 67.8)` ほぼ白 |
| `--card`               | カード背景               | 白                               | ダーク                           |
| `--card-foreground`    | カード内テキスト         | ほぼ黒                           | ほぼ白                           |
| `--popover`            | ポップオーバー背景       | 白                               | ダーク                           |
| `--popover-foreground` | ポップオーバー内テキスト | ほぼ黒                           | ほぼ白                           |

#### セマンティックカラー

| トークン        | 用途             | 使い方                         |
| --------------- | ---------------- | ------------------------------ |
| `--primary`     | メインアクション | ボタン、リンク、アクティブ状態 |
| `--secondary`   | 補助アクション   | セカンダリボタン、タグ         |
| `--muted`       | 控えめな背景     | 無効状態、補足テキスト         |
| `--accent`      | 強調背景         | ホバー、選択状態               |
| `--destructive` | 危険操作         | 削除ボタン、エラー             |

#### チャートカラー

| トークン    | 値         |
| ----------- | ---------- |
| `--chart-1` | 最も明るい |
| `--chart-2` |            |
| `--chart-3` |            |
| `--chart-4` |            |
| `--chart-5` | 最も暗い   |

#### UI カラー

| トークン   | 用途                     |
| ---------- | ------------------------ |
| `--border` | ボーダー                 |
| `--input`  | 入力フィールドのボーダー |
| `--ring`   | フォーカスリング         |

#### サイドバーカラー

`--sidebar-*` でサイドバー専用のカラーを定義。メイン UI と独立して調整可能。

### 角丸（Border Radius）

ベース値 `--radius: 0.625rem`（10px）から計算。

| トークン       | 計算    | 実値（目安） |
| -------------- | ------- | ------------ |
| `--radius-sm`  | `× 0.6` | 6px          |
| `--radius-md`  | `× 0.8` | 8px          |
| `--radius-lg`  | `× 1.0` | 10px         |
| `--radius-xl`  | `× 1.4` | 14px         |
| `--radius-2xl` | `× 1.8` | 18px         |
| `--radius-3xl` | `× 2.2` | 22px         |
| `--radius-4xl` | `× 2.6` | 26px         |

### フォント

| トークン         | 用途                 |
| ---------------- | -------------------- |
| `--font-sans`    | 本文                 |
| `--font-mono`    | コード（Geist Mono） |
| `--font-heading` | 見出し               |

## コンポーネント使用規約

### Button

6 バリアント × 8 サイズ。`@base-ui/react` ベース。

#### バリアント

| バリアント    | 見た目                | 用途                                           |
| ------------- | --------------------- | ---------------------------------------------- |
| `default`     | 塗りつぶし（primary） | メインの CTA。1画面に1つが理想                 |
| `outline`     | 枠線のみ              | セカンダリアクション（キャンセル、フィルタ等） |
| `secondary`   | 薄い背景              | 補助操作                                       |
| `ghost`       | 背景なし              | ツールバー、サイドバー内のアクション           |
| `destructive` | 赤系背景              | 削除、解除など破壊的操作                       |
| `link`        | アンダーライン        | インラインリンク風ボタン                       |

#### サイズ

| サイズ    | 高さ | 用途                             |
| --------- | ---- | -------------------------------- |
| `xs`      | 24px | コンパクトな操作（テーブル内等） |
| `sm`      | 32px | サブアクション                   |
| `default` | 36px | 通常のボタン                     |
| `lg`      | 40px | フォーム送信などの主要アクション |
| `icon`    | 36px | アイコンのみ（正方形）           |
| `icon-xs` | 24px | 小さいアイコンボタン             |
| `icon-sm` | 32px | 中アイコンボタン                 |
| `icon-lg` | 40px | 大アイコンボタン                 |

#### 使い分けルール

- ページ内のメイン CTA は `default` バリアントを1つだけ
- 並列する操作は `outline` または `secondary`
- 削除は必ず `destructive` + 確認ダイアログ
- テーブル行内は `ghost` + `icon-xs` または `icon-sm`

### Badge

6 バリアント。ステータス表示に使う。

| バリアント    | 用途                 | 例                   |
| ------------- | -------------------- | -------------------- |
| `default`     | アクティブ・完了     | 承認済み、有効       |
| `secondary`   | 中立的なラベル       | 下書き、待機中       |
| `destructive` | エラー・失効         | 却下、無効、期限切れ |
| `outline`     | 情報ラベル           | カテゴリ、タグ       |
| `ghost`       | 控えめなラベル       | 補足情報             |
| `link`        | クリック可能なラベル | フィルタタグ         |

#### ステータスバッジの色マッピング

業務上のステータスと Badge バリアントの対応。

| ステータス                 | バリアント    | 例                     |
| -------------------------- | ------------- | ---------------------- |
| 承認済み・完了・有効       | `default`     | 申請承認、出勤中       |
| 申請中・レビュー中・処理中 | `secondary`   | 休暇申請中、経費申請中 |
| 却下・失効・エラー         | `destructive` | 申請却下、期限切れ     |
| 下書き・未提出             | `outline`     | 下書き保存             |

### アイコン体系

Lucide React を使用。以下の使い分けに従う。

#### アクションアイコン

| アイコン    | 用途           |
| ----------- | -------------- |
| `Plus`      | 新規作成       |
| `Pencil`    | 編集           |
| `Trash2`    | 削除           |
| `RefreshCw` | 更新・リロード |
| `Settings`  | 設定           |

#### ナビゲーションアイコン

| アイコン                       | 用途               |
| ------------------------------ | ------------------ |
| `ArrowLeft`                    | 戻る               |
| `ChevronLeft` / `ChevronRight` | ページネーション   |
| `ChevronDown`                  | ドロップダウン展開 |

#### ステータスアイコン

| アイコン       | 用途                                  |
| -------------- | ------------------------------------- |
| `CheckCircle2` | 成功・完了                            |
| `XCircle`      | エラー・失敗                          |
| `Loader2Icon`  | 読み込み中（spin アニメーション付き） |

#### 機能別アイコン（サイドバー）

アイコンの重複を避け、各メニュー項目に固有のアイコンを割り当てる。

| メニュー   | 推奨アイコン     |
| ---------- | ---------------- |
| 従業員管理 | `Users`          |
| 勤怠       | `Clock`          |
| 休暇       | `CalendarOff`    |
| 経費       | `Receipt`        |
| 申請       | `FileText`       |
| 承認受信箱 | `InboxIcon`      |
| 目標       | `Target`         |
| 感謝       | `HeartHandshake` |
| 研修       | `GraduationCap`  |
| ナレッジ   | `BookOpen`       |
| 通知       | `Bell`           |
| 設定       | `Settings`       |

## レイアウトパターン

### 一覧ページ

```
PageHeader（タイトル + アクションボタン）
├── フィルタ / 検索（任意）
├── Table
│   ├── SortableTableHead
│   └── テーブル行
└── TablePagination
```

構成ルール:

- PageHeader の `actions` にメインの CTA（新規作成等）を配置
- テーブルは `Table` コンポーネントを使う
- ソート可能なカラムは `SortableTableHead` で統一
- 0 件時は `EmptyState` を表示
- ページネーションは `TablePagination` で統一

#### 監査・証跡画面の例外

- read 自体が監査イベントを生む導線は、viewport や hover だけで記録を増やさないよう、すべての `Link` に `prefetch={false}` を指定する
- opaque cursor は値や方向を解釈せず、`TablePagination` ではなく前後 cursor 専用のナビゲーションを使う
- CSV は Server Action や RSC の payload に載せず、`Cache-Control: no-store` の Route Handler から API response body を stream する
- IP や証跡 JSON は専用 permission で保護した詳細画面にだけ表示し、JSON は項目別に初期折り畳みとする

### 詳細ページ

```
PageHeader（タイトル + 戻るボタン + アクションボタン）
├── Card
│   ├── CardHeader
│   └── CardContent
│       └── DetailField のグリッド
└── 関連データセクション（任意）
```

構成ルール:

- `BackButton` で前のページに戻れるようにする
- 詳細データは `DetailField`（dt/dd）で表示
- 編集・削除は PageHeader の actions に配置
- ローディング中は構造化スケルトンを表示

### 新規作成 / 編集フォーム

```
PageHeader（タイトル + 戻るボタン）
├── Card
│   ├── CardHeader
│   └── CardContent
│       └── フォームフィールド
└── フッター（送信ボタン + キャンセル）
```

構成ルール:

- フォームの送信ボタンは `default` バリアント
- キャンセルは `outline` バリアント
- バリデーションエラーは各フィールド直下に表示
- 送信中はボタンに `Loader2Icon` のスピナーを表示

## 共通コンポーネント

### PageHeader

全ページの上部に配置。タイトルと操作ボタンを含む。

```tsx
<PageHeader
  title="従業員一覧"
  actions={
    <Button>
      <Plus /> 新規登録
    </Button>
  }
/>
```

### EmptyState

データが 0 件の場合に表示。アイコン + メッセージ + CTA を含む。

```tsx
<EmptyState
  icon={Users}
  title="従業員がいません"
  description="最初の従業員を登録しましょう"
  action={<Button>新規登録</Button>}
/>
```

### ListSkeleton

一覧ページのローディング表示。テーブル形式のスケルトン。

### FetchError

データ取得エラー時の表示。再読み込みボタン付き。

## レスポンシブ対応

### ブレークポイント

Tailwind のデフォルトブレークポイントを使用。

| プレフィックス | 幅     |
| -------------- | ------ |
| `sm`           | 640px  |
| `md`           | 768px  |
| `lg`           | 1024px |
| `xl`           | 1280px |

### レスポンシブルール

- モバイル（`< sm`）: サイドバーは Sheet で開閉。テーブルは横スクロール
- タブレット（`sm` 〜 `lg`）: サイドバーは折りたたみ可能
- デスクトップ（`> lg`）: サイドバーは常時表示

## ダークモード

`ThemeProvider` で管理。`system` / `light` / `dark` の3モード。

### 注意事項

- カスタムカラー（`bg-destructive/10` 等の透過度）はダークモードで別途確認が必要
- `border-border` と `bg-input` はダークモードで oklch alpha を使用
- チャートカラーはライト・ダーク共通

## アクセシビリティ

- フォーカス管理: `focus-visible:border-ring focus-visible:ring-[3px]` で統一
- aria 属性: `aria-invalid` でエラー状態のスタイルを自動適用
- カラーコントラスト: WCAG AA 基準（4.5:1）を目標
- キーボード操作: 全インタラクティブ要素がキーボードで操作可能であること
