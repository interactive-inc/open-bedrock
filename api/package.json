{
  "name": "api",
  "type": "module",
  "exports": {
    "./app": {
      "types": "./dist/app.d.ts",
      "default": "./src/app.ts"
    }
  },
  "scripts": {
    "build:types": "tsc -p tsconfig.build.json",
    "check": "bun run build:types && tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "bun test",
    "db:migrate:local": "wrangler d1 migrations apply open-karte --local",
    "db:migrate": "wrangler d1 migrations apply open-karte --remote",
    "db:seed:local": "bash scripts/seed.sh --local",
    "db:seed": "bash scripts/seed.sh --remote"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.27",
    "jose": "^5.10.0",
    "yaml": "^2.9.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3",
    "wrangler": "^4.107.0"
  }
}
