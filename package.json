{
  "name": "open-karte",
  "private": true,
  "workspaces": [
    "api",
    "cli",
    "web"
  ],
  "type": "module",
  "scripts": {
    "build:api-types": "bun run --filter api build:types",
    "typecheck": "bun run build:api-types && bun run --filter web check && bun run --filter cli check",
    "check": "vp check"
  },
  "devDependencies": {
    "@interactive-inc/claude-funnel": "^0.53.0",
    "vite-plus": "^0.1.23"
  },
  "packageManager": "bun@1.3.14"
}
