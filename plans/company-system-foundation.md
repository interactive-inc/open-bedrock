# Company と System の基盤完成条件

会社の基盤APIを完成させ、共有部分を両製品へ取り込む。機能の部品数、共有ファイルの一致、限定テストの成功だけでは完了としない。

## 残作業の順序

完了範囲は以下のまとまりで管理し、同じ未接続経路を別名の作業として増やさない。個別の不具合修正は、そのまとまりの受入条件へ追加する。

- Companyの正本統合: 既存の所属・上長履歴を確認して移行し、上長本人の退職時の再割当、責務・役職・Account対応の公開APIと既存writerを統合する。雇用主法人・拠点・会社設定の参照と、全期間の参照整合性を同じ正本で検証する。入社から異動・退職・再入社までの表示と資格が一致することを完了証拠とする。
- 判断と実行: 人事申請で成立しているCompany資格・System Task・実行許可の接続を、経費と稟議へ適用する。現状の経費承認は管理権限で対象範囲を補い、稟議承認は保存済みの承認者IDから直接状態を変える。表示した提案版、資格失効、自己除外、委任、実行直前の競合と一回実行を実際の業務変更で検証する。
- 入退社の後続処理: 人事保存はCompany outboxへ追記するが、本製品のWorker入口はfetchのみで、このoutboxを配送する経路がない。実行入口からSystemのlease・再送・dead letter・再投入へ接続し、資産・アクセス・手続きへの引渡しと結果を追跡する。
- 情報と運用: field・purposeの評価部品を永続的な設定と実際の参照・export・機械操作へ適用する。保持・hold・開示の設定を保存・参照・削除経路で強制し、外部結果の受領・照合と合わせて迂回経路を検証する。
- 受入と取り込み: 上の各まとまりを実handlerとDBで通し、必要な画面・CLI・機械呼出しも同じ契約へ追従させる。両製品の共有source・metadata、追記migration、関連テスト・型・境界・生成物を確認して取り込む。本番反映の証拠とローカル検証は区別する。

静的に確認した未接続箇所の出典は、CompanyのAssignment journal、経費と稟議の承認Application、人事保存のoutbox追記、APIのWorker入口である。保持・開示などの未実装条件はcapability mapと照合し、実装開始時に対象の保存・参照経路を再確認する。

## Company の正本

- Person、Employee、Employment、組織、所属、責務、Account対応の公開APIと既存業務経路が同じ会社上の事実を変更・参照する
- 雇用主法人、拠点、組織単位の関係を定義し、必要な参照を型・永続化・APIで検査する
- 初期化、外部identity登録、人事発令、氏名変更、訂正、取消が正本へ接続し、片方の保存先だけを更新しない
- 既存データを保全する移行と、不整合・履歴不足を検出する検証を備える。未知の過去を推測して補わない

## 時点と競合

- 入社前、在籍、休職、復職、退職、再入社を会社timezoneの半開期間で判定する
- 将来の変更を予約しても発効前の資格を変えず、発効後は表示・認可・判断者解決が同じ結果になる
- 最新訂正と取消を反映し、欠落、重複、所有者不一致を現在値へのfallbackで隠さない
- 履歴、業務表示、監査、command receiptを原子的に確定し、再送・競合・部分失敗を検証する

## 主体・資格・承認・実行

- Human、Agent、Service、Connectorの許可範囲を実行時に強制し、人の確認を機械主体が代替できない
- Companyの判断資格をSystemのTaskへ接続する。技術的管理権限だけで会社上の判断資格を補完しない
- 承認対象の版とdigest、実行許可、実行直前の再評価、一回実行を実際の業務変更へ接続する
- 人事発令、経費、稟議を通して、本人除外、資格失効、委任、訂正後の承認失効、再送を検証する

## イベントと後続処理

- 入退社イベントを保存するだけでなく、稼働する実行経路で後続処理へ配送する
- lease、重複排除、retry、dead letter、再投入、完了結果の記録を一貫して扱う
- 資産・アクセス権・入退社手続きへの接続を会社の正本から行い、失敗時の未完了を追跡できる
- 外部処理は引渡し、結果受領、照合までを検証し、依頼の成功を実行完了と同一視しない

## 機微情報と運用

- 項目・利用目的の制御を実際のAPI、export、機械主体の入口へ適用する
- 保持・hold・開示制御を保存・参照・削除経路で強制し、監査の欠落や機微情報の迂回取得を検証する
- API、CLI、機械呼出しで同じapplication ruleを使い、契約と未実装の制約を一致させる

## 検証と取り込み

- 入社登録から所属・資格・業務利用・異動・退職時のアクセス停止まで、実際のhandlerとDBを通して検証する
- 両製品のCompany・Systemディレクトリ全体、manifest、lockを一致させる
- migrationは追記し、適用済みファイルの再採番・変更を行わない
- 両製品の影響範囲のテスト、型検査、境界検査、migration検証と必要な全体検査を実行する
- 共有先の並行作業を混入させず、取り込み済みの差分とGit状態を確認する
- 各条件を現在のsource・テスト・実行結果で再監査し、未接続・未検証が残れば目標を継続する

## 会社初期化の接続

- 初期化を確認済みの会社名・代表者名・言語・timezone・会計年度、雇用日・区分、明示した責務から行う。管理権限から責務を補わず、会社情報を入社日へさかのぼって補わない
- ルートOrgUnitの元の期間を保全し、確認当日からの会社profileと組織名を公開履歴へ接続する。既存台帳、在籍・所属・責務、監査、再送結果を一つのtransactionで保存する
- 再送用のキーと確認済みJSONをCLIからAPIへ引き継ぐ。Companyの競合を成功に変換せず、opaqueな従業員IDを受け取る
- 従業員登録前のCompany初期化はSystemの認証と管理資格を使う。通常のCompany操作には引き続き従業員sessionを要求する
- 組織と所属の隣接する有効期間をつなぎ、親・所属・責務を参照できるようにする。空白、取消、別の所有者による補完、既存参照を孤立させる訂正は拒否する
- 会社名・代表者名が事前設定された初期状態では、確認済み入力と既存値が一致する場合だけ登録する。不一致と保存直前の変更は拒否し、製品ごとの初期schemaを同じ空文字へ書き換えない
- Companyprofileは初期化後の更新経路も公開履歴へ接続する。公開所属・責務・Account対応と既存台帳の全writer、製品固有の初期登録経路の統合は未完成である
- 初期化の準備後に起きるSystem権限変更の同一transactionでの検査、機械Principalと最初の従業員の対応条件も追加の確認対象とする。認証時の管理権限検査だけを基盤全体の認可完成と数えない

- 変更後の本製品API全体は3,349件成功・既存re-export検査1件失敗、CLI全体は357件成功した。共有先はCompany・System・関連compositionの982件が成功した。両製品の実APIで初期化・再送・現在の管理資格失効を検証し、共有testでは全取消・履歴境界・既存プロフィールの不一致と保存直前の競合を検証した
- API・Web・CLIと共有先の型検査、共有source・境界・migration・seed検査は成功した。全体format・lintはエラー0件で、既存のUI警告3件が残る。Company 429ファイルとSystem 500ファイル、共有metadata、8本の追記migrationは一致する。本番DBへの適用と本番D1の実行制限は未検証である

## 現在の検証

- 在籍認可、従業員一覧、Accountに対応する承認候補の参照を、最新の期間履歴と会社営業日へ接続済み
- 外部従業員登録はSystem Account、Employee、Employment、初期の人事記録、期間履歴を一つのbatchで作成する
- 退職発効、休職・復職、訂正、取消、重複、欠落、初期記録の原子性、Account参照に関するテストは成功
- 会社の初期化と新規入社発令を公開Person・Employee・Employmentへ接続済み。共有先の新規Account作成・発行と新規従業員の一括登録も同じ初期resourceへ接続し、雇用区分と発効日・状態を保全する。既存従業員への雇用追加、招待登録と過去データの移行は追加の検証・接続が必要
- 公開Person・Employee・Employmentのwriteを、業務台帳、人事記録、雇用・状態の期間履歴へ同じtransactionで接続済み。将来の休職・復職・退職、訂正・取消、再送、保存失敗、再入社の空白を実際のhandlerから検証済み
- 接続済みEmployeeの名簿は、氏名・従業員番号・存在を公開resourceの有効日で読む。参照期間の空白・短縮、雇用重複をcommand確定時のDB制約で拒否する
- 既存台帳にorganization列がないため、Employee・Employmentの公開writeは既定organizationへ限定している。複数organizationの従業員を混在させる機能は未完成
- 公開resourceへ接続済みのEmployeeでは、既存の休職・復職・退職・再入社・訂正から公開Employment履歴へ反映する。発令・期間・監査・公開履歴を同じtransactionで確定し、再送で履歴を増やさず、公開writeとの競合で一方だけを確定する
- 公開雇用の改訂を専用の人事記録として読み出せる。通常の復職で前の休職を訂正済みにしない。追記migrationは既存人事記録の全列・rowid・一意制約と更新削除禁止を保全する
- 外部同期をCompanyの共通APIへ移し、機械主体・provider scope・付与可能な初期roleを検査する。新規Account・公開Person・Employee・Employmentと期間履歴、氏名・email更新、操作主体を持つ監査・command receiptを原子的に保存する。雇用日・区分は入力を必須にし、email一致だけでAccountへ接続しない。外部版とCompany版の競合、再送、監査失敗、準備後の権限・credential失効と付与先role変更の拒否を実DBで検証した
- 未接続の既存台帳、その他の氏名変更writer、組織・所属・責務・Account対応の統合、Account表示名の将来変更、System側の完成条件は未完了。入社・再入社・その訂正の契約区分入力は公開APIと同じ値を必須にした
- 機械access tokenを発行元credentialへ接続し、両製品のAPI認証でAccount状態・token版、Principalへの所属、credential失効・期限、Connector停止を再検査する。別credentialの継続、別Accountのcredential拒否、権限失効、発行処理中のAccount・Connector変更を検証済み。発行元のない旧機械tokenは再発行が必要になる
- 退職発令の確定・再送から既存tokenの認可までを、実際のApplication・DB・業務handlerで検証済み。退職日中は200、翌日は401
- 新規登録の公開履歴保存失敗ではAccount・従業員・期間も取り消す。登録の再送で公開履歴を増やさず、準備後のCompany版競合は409を返す。会社の版が進んでいる場合は初期化を重ねない
- 本製品のAPI全体は固定Bun版で3,349件成功。変更前からあるUI生成ファイルのre-export検査1件は失敗する
- 共有先のCompany・System全体とAccount表示名・初期化のcompositionは982件成功、失敗0件。Company 429ファイルとSystem 500ファイル、manifest、lockは完全一致
- API・Webと共有先の型検査、共有source・境界検査、変更箇所のformat・lintは成功。本製品の全体lintはエラー0件で、未変更のUI生成ファイルにある警告3件が残る
- 両製品の実際のAPI入口から、外部identity登録とprovider scope違い・credential失効の拒否を検証済み。本製品では公開Company APIの読取、更新、再送、旧共有キー・人のtokenの拒否、通常Company入口の認証維持も検証済み。旧同期APIと入力契約は置き換わる
- 218件の新規従業員を一つのbatchで登録し、654件の公開resourceと218件のcommand receiptを保存するSQLiteテストは成功。本番D1での実行時間と資源制限の検証は未完了であり、大規模一括処理の本番実行を保証しない
- 変更は両製品の共有sourceと追記migrationへ反映済み。残る完成条件の実装と全経路の再監査を継続する

- 汎用申請の保存済み候補が、提出後に上司資格を失っても承認できる不具合を再現し、判断時点の資格再検査へ接続した。有効な代理承認と期限後の追加候補を維持し、失効した委任元の代理承認・否認は拒否する。従業員番号変更と組織版を変えない休職が再検査後に入る競合でも、証言・Task・Caseを確定しない
- 公開Companyの責務・役職・合議体からTaskへ変換する処理を、汎用申請と人事申請が共有するTask生成へ接続した。金額scopeは不変の申請payloadから評価し、定足数・委任規則を固定する。既存の経費・稟議の承認と実行時再評価、従来の組織条件による初回Task生成の競合も引き続き完成条件に含める

- 公開Companyの規程から汎用申請を提出し、合議の二名承認まで実HTTPで検証した。金額範囲外、提出後の責務取消・必要人数変更、候補生成後の取消競合を拒否する。個人・役職、公開対応と業務対応の不一致、機械Principalの候補・証言拒否も検証した。人事申請も公開責務から発令完了まで検証した。複数organizationへの拡張、既存台帳の統合は残る

- 公開責務の設定はAPIとJSON定義から利用できる。基本workflow editorの人数欄はCompanyの合議規程による表示へ未対応であり、編集UIの整合も完成条件として残す

- 合議の否定票も実APIで検証し、一票では審議を継続し、残る賛成可能数が必要数を下回った時だけ否決する。既存の全体検証に加えて、追加した二経路も成功した

- 人事発令の全承認段階を実行直前に再評価し、責務取消、Account停止、機械Principal、委任の期限・取消、承認内容の不一致を拒否する。確認後のCompany・System状態変更も同じ実行batchで拒否し、公開雇用・人事記録・実行許可・完了記録を取り消す。実行できる利用者は現在も有効な承認者に限定し、閲覧者の再試行では実行しない。保存失敗をHTTP成功へ変換していた不具合も修正した

- 人事の実HTTPでは通常発令、一回実行、再送、責務・Accountの失効、確認後の競合、閲覧者の拒否、保存失敗からの再試行を検証した。共有テストでは複数段階、代理判断と取消、内容・digest・Case・seriesの不一致を検証した。機械主体による承認済み業務の実行入口は引き続き未完成

- 汎用申請と人事申請の承認・否認は、表示した提案版・digest・Task key・roundを必須にした。変更のない本文の再提出、次段階への進行、照合後の差戻し競合でも以前の判断を転用しない。Webは詳細の本文と同じ参照を送信し、CLIも明示した参照を送り、409を最新版への自動再送で隠さない。確認なしの旧POST契約は受け付けない

- 提案参照の変更時の検証では、本製品APIは3,262件成功・既存re-export検査1件失敗。CLIは333件、共有先Systemは655件、Webの詳細・フォーム・ServerActionは8件成功。共有Companyのsourceは前回の全体検証から変更していない。Reactの移動ファイルを含む差分検査は旧path参照で失敗したため、変更前後の一時コピーで検査し、両方とも検査完了・指摘0件だった

- 入社・再入社とその訂正に雇用区分の明示を必須とし、承認対象・発令履歴・業務台帳・公開雇用を一致させた。新規登録のWeb・CLIと再入社フォームも明示した区分を送る。CLIの新規登録は冪等キーを送り、再試行用に同じキーを指定できる。区分を持たない旧提案は本文を変更せず409で拒否し、退職済みの初期状態には契約区分や雇用期間を作らない

- 雇用区分の変更後、本製品APIは3,270件成功・既存re-export検査1件失敗、CLIは339件、Webのフォーム・ServerActionは7件成功。共有先の全体検査で2件が時間制限に達したが、関連23件の単独実行と全体の再実行885件は制限を変えず成功した。Reactの変更前後の比較は両方完了し、既存のフォーム複雑さの警告1件と数値は増えていない。実ブラウザでの確認は未実施

- 名簿からの氏名変更と本人の電話番号変更を公開Personの履歴へ接続した。表示と同じ会社版・人物版・営業日、変更理由、冪等キーをWeb・CLI・APIで引き継ぐ。従業員表示とAccount表示名も同じtransactionで保存し、古い画面、同時更新、Account表示名の保存失敗、翌日の再送、将来予約された区間の保全を検証した。既存の台帳だけを上書きするRepository操作は削除し、その検証を共有の実HTTPテストへ移した

- 人物情報の更新では、本製品の登録・実認証・権限合成から本人profileまで検証した。共有テスト11件、WebのフォームとServerAction13件、CLI全体347件が成功した。本製品のAPI全体は3,279件成功し、既存re-export検査1件は失敗する。共有先はCompany・Systemと表示名compositionの900件が成功した

- Reactの変更前後の検査は両方完了し、EmployeeDetailの複雑さの警告1件とlocale変更の認証指摘1件は同じ内容・数値だった。新しい診断は残っていない。実ブラウザでの操作確認は未実施。公開Personに未接続の従業員は編集不可として対応確認を求めるため、既存データの移行、製品固有writerの接続、Account表示名の将来発効への対応を継続する

- 既存従業員を公開正本へ接続するAPIとCLIを追加した。現在値を過去へ補わず、確認済みの人物履歴と保存済みの全雇用・在籍区間を照合する。元の台帳・全期間revisionを変更せず、公開履歴・接続・移行証跡を一つのtransactionで確定する。履歴不足の既存データ、部分接続、既存Personへの名寄せ、複数organizationは引き続き自動移行の対象外になる。

- 既存従業員の移行では、共有Companyの実routeで移行・本人連絡先・公開雇用の更新を検証し、共通人事発令処理で復職・退職・再入社を検証した。本製品のAPI入口では認証・管理資格・参照・保存・再送も検証した。氏名・連絡先・番号・契約区分・所有者・期間の不一致、Account表示の不一致、保存直前の変更、退職済み契約、同時再送、保存失敗による全取消、再試行を検証した。

- 移行追加後の本製品API全体は3,299件成功、既存re-export検査1件失敗。CLI全体351件と、API・CLIの型検査、境界・共有source・migration・seed検査は成功した。

- 共有先の移行テスト19件と、Company・System・関連composition全体919件は成功、失敗0件。Company 410ファイル、System 500ファイルと各共有metadataは一致した。本番データへの適用、本番D1の資源制限、履歴不足の既存台帳の修復は未検証・未完了であり、基盤全体の目標は継続する。

- 公開組織変更の成功済み再送が会社版の検査で409になる不具合と、初回receipt照会後に同じ依頼が確定した場合も409になる競合を再現した。組織構造の検査をcommand保存経路へ移し、保存済み結果を先に照合する。検査・保存で失敗した場合も同じcommandの確定結果を確認し、別内容の同じキーと未成功の古い版を区別する。

- 既存の組織一覧・ツリー・詳細・所属者・本人所属と作成・更新・削除に、既定organizationへのアクセス検査を追加した。別organizationの管理権限だけでは参照・変更・成功済み依頼の再送ができない。プロフィール変更も対象organizationを検査する。公開組織resourceと期間台帳、所属・責務・Account対応の統合は引き続き完成条件として残る。

- 組織修正後のAPI全体は3,311件成功・既存re-export検査1件失敗、共有先のCompany・Systemと関連compositionは931件成功した。共有先の境界検査で見つかった既存のプロフィールschemaの配置違反も、入力契約を変えず共通定義へ移して解消した。移動後の人物更新・組織変更・構造契約は本製品45件、共有先37件成功した。

- 既存組織の全期間revisionを公開APIへ接続するAPIとCLIを追加した。元の期間ID、訂正履歴、操作主体・理由・証拠を保全し、親から順に管理者が確認して接続する。公開・既存APIの変更は同じtransactionで保存し、片側だけの更新をDBで拒否する。

- OrgUnitは期間IDと組織の同一性を区別し、最新revisionを期間全体の訂正として評価する。将来の改組は別期間として登録し、既存APIの変更は会社営業日に有効な期間だけを対象にする。開始日訂正で旧期間が復活する不具合と、将来・取消済み組織を現在表示に混ぜる不具合を修正した。

- 所属・責務・Account対応の保存経路、会社初期化で使うルートOrgUnitの公開正本への接続、既存の組織編集画面とCLIが表示した期間・版の照合、複数organizationの業務台帳は引き続き完成条件として残る。

- 組織接続後の本製品API全体は3,327件成功・既存re-export検査1件失敗、CLI全体は355件成功した。共有先はCompany・Systemと関連compositionの946件が成功し、失敗0件。役職の承認候補テストは既存ルートの履歴確認を経て公開組織へ接続するfixtureに更新した。欠落履歴のテストは、両製品の既存スキーマから対象の制約を特定して破損を再現する。

- API・Web・CLIと共有先の型検査、共有source・境界・migration・seed検査は成功した。全体format・lintはエラー0件、既存のUI生成ファイルの警告3件が残る。Company 423ファイル、System 500ファイルと共有metadata、9本の追記migrationは一致する。本番DBへの適用と本番D1の資源制限は未検証であり、基盤全体の目標を継続する。

## 会社プロフィールの接続

- 会社情報の既存APIと公開APIを同じ時点履歴へ接続する。接続前情報は保全し、公開履歴の空白・取消や未知の代表者を旧情報で補わない。会社profileのidentityを一つに制限し、接続後の旧保存先への直接更新を拒否する。
- 表示版、会社営業日、確認理由、冪等キーを検査し、履歴・監査・変更前情報・再送結果を一括保存する。現在の編集は将来予約と区間終端を保全する。
- 共有先の会社情報画面は全項目の確認と理由を要求し、古い表示を検知する。再送キーを保持し、保存成功後の読込失敗を保存失敗と区別する。会社概要の読取にも会社営業日を渡す。
- 会社timezone・会計年度の実行設定への統合、全業務経路の正本への接続、実ブラウザでの確認と本番DBへの適用は未完了である。基盤全体の目標を継続する。

- 会社プロフィールの接続後、本製品のAPI全体は3,361件成功・既存のre-export検査1件失敗。共有先のCompany・Systemと関連APIは982件、追加の初期化経路は14件成功した。画面の4件では、保存後に最新情報を取得できない間も古いキャッシュを無効とし、再取得後に表示・キャッシュを更新することを検証した。
- 共有先の全migrationを使うテストは既定の5秒を超えたため、再実行と全体検証では20秒の制限を指定した。実行時間の制約を変更した結果であり、本番D1の性能検証ではない。
- React検査の変更前後比較は、変更した画面と関連コードを保持し、無関係な画面とテストを除いた一時コピーで両方完了・指摘0件となった。リポジトリ全体を含む検査は解析対象5,000ファイルの制限に達する。実ブラウザでの確認は未実施である。

## 上長関係の循環検査

- 複数の上長関係を持つ従業員について、最後の関係で他の関係を上書きし、登録順によって循環を保存できる不具合を実APIで再現した。新規登録、既存関係の変更、将来期間の重複の三経路で拒否すべき変更が保存されていた。
- 同一時点の全関係から循環を判定する処理を共有し、資格解決でも経路の列挙を廃止した。正常な兼務・合流、冪等な再送、期間の境界を維持し、循環のある変更では会社版・履歴・receiptを保存しないことを検証した。関連18件が成功し、登録順の全六通りと約四万頂点の分岐も検査した。
- 所属の統合では、公開AssignmentとReportingRelationが分かれている一方、既存所属の期間履歴は上長も保持する。責務とAccount対応も含め、既存履歴を保全した接続と全writerの統合が残る。循環検査の修正だけでCompany全体を十分とは判断しない。
- 本製品API全体は3,366件成功・3件失敗だった。既存のre-export違反1件と、未変更の移行テスト2件の5秒timeoutである。移行テストのファイル全体19件は20秒を指定した再実行で成功した。時間制限を変更した再検証と全体実行の結果を区別し、本番D1の性能検証として扱わない。
- 共有先のCompany一括実行は外部identity同期の三件が20秒の上限に達したため中断した。プロセスを分けた関連検証では五ファイル29件が成功したが、新しいHTTP検証三件と手続き資格一件も同じ上限に達した。新しいHTTP検証の実行サンプルではSQLiteのschema構築中だった。
- 残った六件は180秒の上限で再実行し、全件成功した。この実行での各テストは約二〜三秒だった。先に成功した外部identityのメール変更も含め、共有先の関連36件を検証した。通常の上限でCompany全体が完走した結果ではなく、一括実行の失敗と分離した再実行を区別する。
- 両製品の型・共有source・境界検査と変更箇所のformat・lintは成功した。本製品の全体lintはエラー零件で既存のUI警告三件が残る。Company 439ファイル、System 500ファイルと共有metadataは一致する。
- この時点の公開組織変更の横断検証はheadを読んでいたため、過去版と将来予約を組み合わせた指揮命令を続けて再監査した。

## 指揮命令の全履歴

- 将来の上長変更・取消を登録した後、その前の期間に循環する関係を登録できることを実HTTPで再現した。参照APIは以前の版を返す一方、変更の検証ではその版を読んでいなかった。
- 確認した会社版以下の全改訂から、有効な指揮命令の期間を組み立てる値モデルを追加した。将来予約、同日訂正、空白期間、終了・取消後の旧版の不復活を参照APIと一致させる。公開組織変更だけでなく、接続済み組織の既存更新と組織移行も同じ履歴を読む。
- 関連16件が成功した。過去の循環を拒否し、最大revisionの期間だけで見ると循環に見える正常な将来関係は受け付ける。同時更新の一方を拒否し、再試行が循環になる場合も拒否する。履歴読取の失敗では会社版・履歴・receiptを変えず、同じキーで再試行できる。
- 所属・責務・Account対応と全writerの統合、公開resource間の全期間参照整合性、Systemの実行と運用の完成条件は引き続き残る。
- 業務上の管理関係を解決するOrganizationWorkforceSnapshotAdapterは、既存の所属期間にあるmanager_employee_idを読む。公開Assignment・ReportingRelationとの接続と既存履歴の移行が必要であり、今回の公開APIの循環検査だけで業務資格まで接続済みとは扱わない。
- 本製品API全体は3,381件成功・既存のre-export検査一件失敗だった。テストの型の修正後も対象の実HTTP九件は成功した。共有先は値モデル、実HTTP、組織移行・更新、Repository、既存Company APIの五ファイル63件が成功した。
- 全体APIは20秒、共有先の対象検証は180秒の上限を指定した。両製品の型、共有source、境界、変更箇所のformat・lintは成功した。本製品の全体lintには未変更のUI警告三件が残る。実ブラウザや本番D1での確認は行っておらず、所属・資格の統合と基盤全体の目標を継続する。

## 所属から独立した指揮命令の入力

- Workforce資格resolverに、同一時点で解決した指揮命令を必須入力として分離した。同じEmployee・OrgUnitに複数の上長を持てる。空集合を既存の所属上長で補わず、架空の所属も作らない。
- 所属由来の関係は元の期間ID・版・所有者・上長・OrgUnitと照合し、従来の証拠形式を保つ。独立した関係は記録IDと版を持ち、業務へ返す直属上司・指揮命令経路の証拠にも保持する。異なる日付、欠落・非在籍のEmployee、重複した記録、循環を拒否する。
- テストだけが参照していた旧資格resolverを削除し、既存の資格テストを実際の業務から使われるresolverへ移した。複数上長、分岐した経路の決定性、元の所属の不変性、証拠の食い違いも検証する。
- 現在のI/Oは既存の所属履歴から明示的に関係を作る。公開ReportingRelationの時点読取とwriterの統合、既存履歴の移行、Employeeに対する管理範囲判定の統合は未完成であり、今回の入力・証拠の分離だけを公開APIから業務資格への接続完了と数えない。
- 本製品API全体は3,392件成功・既存のre-export検査一件失敗、共有先は資格resolver、証拠変換、判断時の資格再検査、公開雇用と業務投影の四ファイル38件が成功した。全体APIは20秒、共有先の対象テストは180秒の上限を指定した。
- 両製品の型、共有source、境界、変更箇所のformat・lintは成功した。全体lintはエラー零件、未変更のCompanyテスト一件とUI三件の警告が残る。Company 443ファイル、System 500ファイルと共有metadataは一致し、migrationの変更はない。実ブラウザと本番D1での検証、および基盤全体の十分性は未達のまま継続する。

## 公開の指揮命令から業務資格への接続

- 公開APIで上長関係を保存しても、従業員に対する管理権限がfalse、手続きの候補が空になることを実DBで再現した。公開ReportingRelationの有効日と版を読み、管理範囲、配下の一覧、閲覧者との関係、承認候補へ接続した。
- 公開resourceのCompany revisionとlifecycleのorganization revisionを固定する。従業員番号の対応を読む前、内部snapshot、候補解決後の版の一致を検査する。公開履歴の読取失敗、途中の変更では候補を返さず、新しいsnapshotでの再試行を受け付ける。
- 将来変更、過去への訂正、空白期間、取消後の旧上長の不復活を、公開APIへの保存と実際の候補解決で検証した。資格証拠へ公開の関係ID・版とCompany revisionを渡す。
- 未移行の所属と公開関係が同じEmployee・OrgUnitを所有する場合は、上長が同じでも推測で統合しない。重複範囲を拒否し、範囲が異なる関係も合わせて循環を検査する。従来の単数上長欄は一人のときだけ返し、複数人は配列で表す。
- 従来の組織条件を使うTaskにも、候補生成前と生成後のCompany状態を保存時に確認するguardを返す。後続の上長変更でguardが失敗し、同じbatchの変更も巻き戻ること、旧上長が判断時の再検査を通らなくなることを確認した。本製品の汎用申請・人事申請は初回・後続Taskへこのguardを渡す。
- 公開Assignmentの投影、既存所属と指揮命令の移行、全writerの統合、異動・退職・再入社をまたぐ指揮命令の扱いは引き続き未完成である。各製品の業務経路とSystem Taskの接続、実行・配送・保持・開示の完成条件も継続する。
- 最終確認で、直属部下一覧と報告ラインに単数上長・従業員code必須の前提が残っていたため、Employee IDと明示的な全上長の関係へ変更した。codeなし・所属なし・複数上長・共通の上位者を持つ実HTTPテスト、将来codeの発効境界、読取中の変更と再試行を追加した。従業員情報は同じ基準日で読み、両revisionを再確認する。Webはcodeなしの従業員も表示し、存在しない詳細URLへリンクしない。
- 本製品API全体は3,408件成功、変更前からある生成UIのre-export検査一件失敗。共有先は六ファイル74件が成功した。両製品の型・共有source・境界検査、API生成型とWeb・CLI・MCPの追従を確認した。変更したWeb二ファイルはReact Doctorで100点、全体format・lintはエラー零件と既存UI警告三件である。
- Company 447ファイル、System 500ファイルと四つの共有metadataは一致し、migrationに変更はない。共有先の別作業17ファイルを保全した。ブラウザ、本番D1、本番への反映は実施していない。公開所属と既存writerの統合、および基盤全体の十分性は未達のまま継続する。

## 公開所属から業務の期間台帳への接続

- 公開Assignmentが201で保存されても従業員一覧では所属なしになることを実HTTPで再現した。全revisionから有効期間を復元し、組織operationと同じtransactionで期間台帳へ反映するようにした。
- 将来の所属、過去への訂正、空白、取消後の旧所属の不復活、組織の新設と配属の同時保存、主務重複の巻戻し、競合、履歴読取失敗と再試行を検証する。所属期間と公開正本の対応を保持し、期間台帳だけを変更するoperationは完了させない。
- 次は、人事発令からの逆向きの履歴更新と、既存の所属・上長履歴の移行を接続する。公開所属を人事発令で更新できる完成状態を目標に保ち、拒否する保護だけを統合の完成とは扱わない。雇用終了と配属終了を一つの変更で確定する順序も確認する。
- 全体検証で既存の参照整合性テストが存在しない組織へ所属を登録していたため、参照先の組織も同じcommandで登録するfixtureへ更新した。参照元が残る取消の拒否と、人・従業員・雇用・所属の一括取消は引き続き成功する。
- 会社版の初回検査後、所属履歴の読取直前に別commandが確定すると、正しい入力を422で拒否する競合を再現した。投影の準備が失敗した場合も会社版を再確認して409を返し、最新の版と同じ再送キーでやり直せるようにした。別commandの確定結果を保全することと、期間台帳だけの更新が正本照合の制約で拒否されることを検証した。
- 本製品API全体は3,415件成功、変更前からある生成UIのre-export検査一件失敗。共有先は七ファイル95件が成功した。本製品は20秒、共有先は180秒の上限を指定した。両製品の型、共有source、境界、変更箇所のformat・lintと本製品のmigration・seed検査は成功した。本製品の全体format・lintはエラー零件、未変更のUI警告三件である。
- Company 451ファイル、System 500ファイルと四つの共有metadata、追記したmigration六本は一致する。本製品の既存migration 115本、共有先の446本と別作業17ファイルは保全した。ブラウザ、本番DBへの適用、本番D1の性能は未検証である。人事発令からの更新と既存所属の移行、およびSystemを含む基盤全体の十分性は未達のまま継続する。

## 人事発令から公開所属への履歴更新

- 公開APIで作成したOrgUnitへ配属すると、組織codeから作った別IDで検証に失敗することを再現した。所属・責務の期間へ元のOrgUnit IDを保持し、全組織期間と発令の有効日で検証するようにした。
- 接続済み所属を変更する発令から、公開Assignmentの履歴と期間対応を同じtransactionで更新する。異動・役職変更で生じる期間にも公開Assignmentを作り、過去の境界と将来予約を保全する。雇用と所属の変更をまとめて記録し、公開APIによる再更新では元の期間IDを保つ。
- 直接発令と実HTTPでの二名承認からの発令を検証した。会社直下の所属終了、役職変更、異動、訂正、退職、競合、公開履歴の保存失敗による全取消、同じキーの再試行・再送を確認する。競合発令が予期しないエラーになる不具合も修正した。
- 最新の公開Assignmentが取消でも、将来の所属が有効な場合に雇用を取り消せる不具合を再現した。接続済みの全所属期間を雇用期間で包含するDB検査を追記し、参照元が孤立する変更を保存しない。
- 未接続の所属の新規作成、既存履歴の移行、上長とReportingRelation、責務・Account対応の統合は未完成である。公開APIの雇用短縮と所属終了を組み合わせる順序、およびSystemを含む基盤全体の完成条件を継続する。

- 全体検証で再入社の保存順序による退行三件を検出した。新しい公開雇用を作る前に対応を保存していたため、版の検査、公開履歴、雇用と所属の対応の順に保存するよう修正した。再入社と契約区分の訂正、既存従業員の移行後の発令も再検証する。

- 保存順序の修正後、本製品API全体は3,424件成功、変更前からあるUI生成ファイルのre-export検査一件失敗だった。共有先の関連九ファイル125件は成功、失敗零件。本製品は20秒、共有先は180秒の上限を指定した。
- 両製品の型、共有source、依存境界、変更箇所のformat・lintと本製品のmigration・seed検査は成功した。本製品の全体format・lintはエラー零件、未変更のUI警告三件が残る。
- Company 454ファイル、System 500ファイル、四つの共有metadata、追記migration一本は一致する。既存migrationは本製品121本、共有先452本を保全し、共有先の別作業17ファイルは変更していない。ブラウザ、本番DBへの適用、本番D1の性能は未検証であり、基盤全体の十分性は未達のまま継続する。

## 入社と初回配属の接続

- 新規配属が業務台帳では成功しても公開Assignmentが空になることを再現した。公開Employeeと接続済みOrgUnitへの入社・主務配属・兼務追加で、公開履歴と期間対応を同じ発令に保存する。未接続の既存Employeeや所属履歴を自動移行しない。
- 会社初期化で作る最初の所属も、入社日から有効な公開Assignmentへ接続する。元の期間IDと初期化の会社版を保ち、履歴や対応の保存失敗は全変更を取り消す。
- 会社を確認した当日の組織期間境界で所属終了が失敗することと、部署番号を将来変更すると所属を特定できないことを再現した。対象日に有効な組織期間を優先し、部署番号から解決した安定IDで所属・責務を特定する。
- 実HTTPの二名承認による新規入社から雇用・初回所属の公開までを検証する。主務を変えない兼務追加、初期所属の終了と再送、公開履歴・対応の保存失敗による巻戻しも対象とする。

- 本製品API全体は3,429件成功、変更前からあるUI生成ファイルのre-export検査一件失敗。共有先の関連十ファイル141件は成功、失敗零件。本製品は20秒、共有先は180秒の上限を指定した。
- 両製品の型、共有source、依存境界、変更箇所のformat・lintと本製品のmigration・seed検査は成功した。本製品の全体format・lintはエラー零件、未変更のUI警告三件が残る。
- Company 454ファイル、System 500ファイル、四つの共有metadataは一致する。migrationの変更はなく、本製品122本と共有先453本、共有先の別作業17ファイルを保全した。実ブラウザと本番DBでの検証は未実施。残作業を冒頭の五つのまとまりへ整理し、基盤全体の目標を継続する。

## 人事発令と指揮命令の接続

- 公開Employeeの上長付き配属と上長変更を、直属上長のReportingRelationへ接続する。人事発令が管理するEmployee・Employment・OrgUnit・所属種別の対応を記録し、独立した複数上長を維持する。所属期間の上長欄へは複製せず、公開履歴から管理範囲と判断候補へ渡す。
- 役職だけの変更では関係の版を増やさない。異動・所属終了・退職では対応する期間を閉じ、再入社は新しい雇用へ別の関係を作る。未接続所属の上長を引き継ぐ場合は発効日から公開し、それ以前の既存履歴を保全する。
- 現在の上長変更は公開APIで確認した将来予約の境界までに限定する。訂正は元の発令前の履歴を基に作り、同じ関係への後続編集を検知した場合は競合として拒否する。上長変更の要約も公開履歴の変更前の上長から作る。
- 同じ組織の主務と兼務が上長を上書きする問題を再現し、対応と期間の検査を所属種別ごとに分けた。未接続の過去の所属を公開関係の期間の根拠に使う変更も拒否する。
- 公開APIでも所属範囲外の関係と所有者変更を拒否する。所属と関係の同時終了は同じtransactionで確定する。対応は不変とし、雇用・従業員・組織・公開resourceの参照をDBで保護する。
- 訂正による同じ所属期間の再開・再終了で版順序が逆転する問題と、再入社の所属検証が新しい雇用を含まない問題を修正した。直接発令と承認済み発令で、変更後の雇用・在籍状態と所属をまとめて検証する。
- 既存の全上長履歴の移行、上長本人の退職による部下側の再割当、責務・Account対応と全writerの統合は残る。Systemを含む基盤全体の完了条件も継続する。

- 最終検証では本製品のAPI全体が3,441件成功し、未変更の生成UIにあるre-export検査一件が失敗した。共有先は十一ファイル153件が成功した。本製品は20秒、共有先は180秒の上限を指定した。両製品の型・共有source・境界検査、migrationとseed、変更箇所のformat・lintを確認し、本製品の全体lintはエラー零件・既存UI警告三件である。
- Company 457ファイル、System 500ファイルと四つの共有metadata、追記migration九本を一致させた。本製品の既存migration 122本、共有先の453本と別作業17ファイルを保全した。本番DBへの適用、本番D1の性能、基盤全体の十分性は未達であり、残作業を継続する。

## 上長本人の退職と部下側の関係

- 上長の退職発令が成功しても部下側の公開関係が残る問題を実DBで再現した。雇用期間の変更では当事者を部下・上長とする公開関係を合わせて更新する。別の上長との関係と将来予約を保ち、再入社だけで旧関係を復活させない。
- 退職API、訂正、再送、保存失敗、部下側の再割当と同時変更を検証する。訂正で後続の編集を上書きせず、会社版の競合では発令も巻き戻す。
- 組織変更APIへEmploymentを追加し、雇用と指揮命令を一つのcommandで終了できるようにする。関係の全履歴と雇用範囲をDBで照合し、雇用だけの短縮と退職後への関係拡張を拒否する。適用済みmigrationは変更せず追記する。
- 未接続の所属に残る上長履歴の確認・移行と、部下の新しい上長への割当は残る。責務・Account対応、判断と実行、outbox配送、情報と運用の完成条件も継続する。

- 最終版の本製品API全体は3,453件成功し、未変更の生成UIにあるre-export検査一件が失敗した。共有先の関連十ファイル147件は成功、失敗零件。退職API、将来予約、退職訂正、再入社、部下側の同時変更、保存失敗と百件を超える関係の一括終了を検証した。
- 両製品の型・共有source・依存境界、本製品のmigration・seed、共有先のmigration番号の検査は成功した。Company 459ファイル、System 500ファイルと四つの共有metadata、追記migration三本は一致する。既存migrationは本製品131本と共有先462本を変更せず、共有先の別作業17ファイルを保全した。本番DBへの適用と本番D1の性能は未検証であり、基盤全体の目標を継続する。
